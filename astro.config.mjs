import { defineConfig } from 'astro/config';

export default defineConfig({
    // 换成你的 github pages 完整网址
    site: 'https://TomWhite-tgz.github.io',
    devToolbar: {
        enabled: false,
    },
});
